# What does the following code do? Why?

# print('5' + '10')

# Answer: It will concatenate the two strings in the () and output '510' because they are not recognized as
# integers to Python but are defined and recognized as strings..
