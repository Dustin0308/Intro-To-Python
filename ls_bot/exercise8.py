# What will the following code do and why? Don't run it until you have tried to answer.

# b = [1, 2, 3]             # a list 'b' with values of 1, 2, 3 at indexes 0, 1, 2.
# def my_function():        # defines a function to change index[0] which is 0, to 10.
#     b[0] = 10
# my_function()             # applies the changes.
# print(b)                  # prints the new list b = [10, 2, 3]

b = [1, 2, 3]
def my_function():
    b[0] = 10
my_function()
print(b)
