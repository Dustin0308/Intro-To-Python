# Write a program that asks the user for their age in years and then displays their approximate age
# in days.

age = float(input('What is your age? '))
age_in_days = age * 365
print(f'You are {age_in_days} days old!')
